#include "stdio.h"
int main(void)
{
	char c;
	int x;
	float y;
	double z;

printf("The size of char = %d \nThe size of integer = %d \nThe size of float = %d \nThe size of double = %d",sizeof(c),sizeof(x),sizeof(y),sizeof(z));
return 0;
}
